# 二期计划：Gitee Go 云端主出票通道 + 本机 12:45 兜底（v2）

> 立项：2026-09-04 ｜ v2 改令：2026-09-07 总司令令**倒置**（原：本机 11:30 主、云影子）
> 状态：M4 执行中——M2/M3 已于 2026-09-07 当日验收（GitHub `14b4de7` 云端主出票首批 + `status` 分支幂等空转审计）；**09-09 云定时首跑全绿 = 云绿连跑 D1/7；09-10 D2 全绿（休战日正常产出）= D2/7**，验收记录见 §七。
> M4 四步验收：**09-09 当日全部通过** ✅——云定时首跑落库（`2325349`）/ status 当日摘要 / 本机 12:45 静默让路（日志 5 秒待命退出）/ CF Pages 镜像随推重建（站点数据 `generatedAt 03:36:50Z` 与云提交同秒对齐）。
> 已定军规：本机 12:45 任务**永久保留为保险丝**（云正常时预检 5 秒退出，零成本）；令牌走 C 案——GH PAT 维持 30 天，10-05 09:55 已挂持久轮换提醒。
> 前置实证：探针仓库 sporttery-probe 16 轮全链路验证（已按 M1 吊销令牌，待删/归档）

## 一、目标与原则

- **目标**：云端为主，「当日必出票」彻底不依赖本机在线；本机降为兜底，云断则补位。
- **原则（v2 军规）**：
  1. 云 Gitee Go 每日 **11:30**（北京）主出票，commit 标 `[cloud-primary]`；
  2. 本机计划任务 `DailyBettingAdvisor` 改 **12:45**：pull 后预检「当日批次已存在→待命退出」，无批次才顶跑，commit 标 `[local-backstop]`；
  3. **引擎零改动**：幂等键 `matchId|pool` + `day.date`=北京生成日 + 无变化跳过——谁先写入谁得当日档案，另一方自动空转；
  4. 令牌只存私密 shadow 库 yml `variables:`；GitHub PAT 30 天过期，Gitee 令牌季度手动轮换（进过 git 历史即视为泄露面）。

## 二、架构

```
Gitee Go（百度云苏州）yml triggers.schedule cron '0 30 11 * * ?'（**6 段 Quartz 秒分时日月周**；社区版定时写在 yml，仅一条；「定时运行」表单是企业版功能）
  └─ gao-jiashun/shadow(私密) .workflow/shadow.yml   # variables 含 GH PAT + Gitee 令牌
       └─ shadow.sh（仓库根）:
            1) clone GitHub 公开库 --depth=1（重试×5，治苏州→GitHub 抖动）
            2) node scripts/daily-advisor.js --selftest —— 两模式共用闸门
            3) SHADOW_MODE=issue：无参跑引擎（回收+出票）→ daily-advice.json 有 diff 才
               commit "[cloud-primary]" → pull --rebase → push GitHub main（重试×5）
            4) 无论成败：执行摘要回写本库 status 分支（每日一 md，日志需登录方可看）
本机 12:45：run-daily-advisor.ps1 → 预检 → 无当日批次才顶跑 [local-backstop]
```

- 产物镜像：本仓库 `shadow/shadow.sh` + `shadow/shadow.yml`（占位符模板、无密钥）；**source of truth = Gitee 私密库**。
- 已知代价：Gitee Go 免费档无 SLA，漏跑/延迟时批次漂到 ~12:45 本机补位；极端竞态（云迟写撞本机批次）由 rebase 冲突自动败者退让，不脏数据。
- 免费额度：每日 1 跑约 3-5 分钟，月耗 ~120 分钟 < 500 分钟个人月额。

## 三、里程碑与现状

| 阶段 | 内容 | 通过标准 | 状态 |
|------|------|----------|------|
| M1 | 探针令牌吊销、实验仓库清理 | 双平台令牌列表清空 | ✅ 09-07（probe 库删除与否待确认） |
| M2 | shadow 私密库 + shadow.sh/yml 推送 + 双新令牌注入 | 云端日志 `selftest PASS (31 用例)` + `result=selftest_only` | ✅ 09-07（修两格式坑：`step:` 键名、参数平铺；密钥改 `config.sh` 走仓库，平台会洗 variables 值） |
| M3 | SHADOW_MODE=issue 真首发 | 云端出当日批次+回写 GitHub + 并发轮幂等空转 | ✅ 09-07：#4 落 `14b4de7 [cloud-primary]`（回收 ¥7.72+休战记录）；#6 `result=no_change`+status 分支建档 |
| M4 | 云定时 `triggers.schedule '0 30 11 * * ?'`（6 段 Quartz，定稿 7418292）；连跑一周；**断云演练**：某天停云端→验证本机 12:45 顶替出 `[local-backstop]` | 一周云绿 + 一次成功顶替 | 🔄 09-07 本机兜底首验✅；09-08 上午定时哑火（5 段 POSIX 被平台静默忽略从未注册，见 §六），当日换 6 段实锤「定时触发」（#9）+ 本机 12:45 顶跑出 `ce22565 [local-backstop]`（断云演练✅歪打正着）；**09-09 定时首跑成功**：11:30:18 准点触发→11:36:58 回写 `2325349 [cloud-primary]`，STALL 护栏实战救场（详见 §七）——云绿连跑 D1/7 起算 |
| M5 | 收尾：CLAUDE.md 通道节 + 记忆同步（09-07 已改）、探针库归档、本文档归档 | 总司令验收 | ⬜ 进行中 |

## 四、风险与对策

| 风险 | 对策 |
|------|------|
| 云定时漏跑/延迟（无 SLA） | 本机 12:45 天然兜底；status 分支留每日执行摘要 |
| 苏州→GitHub 抖动 | clone/push 各重试×5；仍失败则摘要落 status 分支、次日本机为当日主力。~~⚠️ 09-08 #7 单次 clone 挂死 16 分钟~~ **✅ 已解决（09-09 `c99c298`）**：全部 git 网络操作加 STALL 低速掐断 `-c http.lowSpeedLimit=1024 -c http.lowSpeedTime=60`（60s 内 <1KB/s 即 abort），首跑实战前 2 次 clone 被快速掐断、第 3 次成功，全程 6.5 分钟 |
| 双机竞写 daily-advice.json | 错峰 75 分钟 + 先 pull --rebase + 引擎幂等；冲突时云自动落败 |
| 令牌经日志/历史泄露 | 单库最小权限 + 30 天过期 + 季度轮换；泄露窗口攻击面=仅本站仓库文件 |
| Gitee yaml 保存但流水线对象不注册（探针期 issue #IJUFI0） | 兜底：Gitee Go 页「新建流水线→YAML 编辑」指向 .workflow/shadow.yml |
| Gitee Go 日志强制登录 | 未登录浏览器 403，验收日志由总司令本人复制 |

## 五·附：09-08 云定时哑火事件簿（当日闭环）

- **现象**：11:30 无 `[cloud-primary]`；构建历史连 push 事件也不触发（12:44/13:22 两推零动静）。
- **根因链**：`cron '30 11 * * *'`（5 段 POSIX，官方文档示例样式）被平台**静默忽略、定时从未注册**；后探明 Gitee Go 真格式为 **6 段 Quartz `秒 分 时 日 月 周`（周位 `?`）**——`'30 11 * * ?'` 报 `YamlConvertException.cronExpressionError`，`'0 55 13 * * ?'` 通过。
- **注册纪律（血泪）**：cron 改动**必须网页编辑器保存**才重新注册；平台报错弹窗后**仍会把内容写回仓库**（git 端以 HEAD 为准）；转换失败挂起**全部**触发（push/定时全哑），**空内容再保存一次即复活**；报错但内容相同 = 跳过校验的假绿「没报错」。
- **实锤**：15:07:45 #9 构建历史标注「**定时触发**」自动起跑；当日票未缺——12:45 本机保险丝顶跑 `[local-backstop]`（M4 断云演练同日意外完成）。
- **附带发现**：云 `clone GitHub` 可单次挂死 16 分钟+（retry×5 无超时护栏）。

## 六、不做清单（防蔓延）

- 不动 `daily-advisor.js`/前端/数据接口；不复活 WCC/lottery-odds 采集（冻结令不变）；不建 GH 侧新 secret/Action；不再尝试海外反代（CF 出口被 EdgeOne 拦已证伪）。

## 七·附：09-09 云定时首跑验收记录（M4 云绿 D1/7，全链路绿灯）

> 性质：6 段 Quartz cron 定稿（09-08）后**首次正点定时触发**的完整证据链存档。当日双喜：cron 准点 + STALL 护栏（当日 10:21 才提交 `c99c298`）首跑即实战救场。

### 执行时间线（Gitee `status` 分支 `2026-09-09.md` 摘要原文为证）

| 时间 | 事件 | 判定 |
|------|------|------|
| 11:30:18 | 定时触发 `start mode=issue`（node v14.16.0） | ✅ cron 准点，哑火案彻底翻篇 |
| 11:31:18 | clone 第 1 次失败——**恰好 60s 被 STALL 掐断** | ✅ 护栏生效（09-08 同款链路挂 16.5 分钟） |
| 11:33:59 | clone 第 2 次失败，retry 接力 | ✅ fail-fast 让 ×5 重试真正有意义 |
| 11:36:50 | `selftest PASS (31 用例)` + 引擎出票 exit=0 | ✅ |
| 11:36:58 | GitHub 回写成功 → `2325349 [cloud-primary] 2026-09-09` | ✅ 全程 6.5 分钟 |

### 关键佐证

- **STALL 已在 Gitee 侧生效**：摘要中 clone 命令带 `-c http.lowSpeedLimit=1024 -c http.lowSpeedTime=60` 字样 → source of truth（Gitee 私密库）与本仓模板镜像已同步，「镜像是否同步」疑虑销案。
- **当日票面正常**（¥6 / 预算 ¥20，非休战）：T1 2串1 ¥4 @2.37（欧冠 那不勒斯vs阿森纳 客胜@1.51 + 利物浦vs马竞 主胜@1.57）；T2 彩蛋 ¥2 @3.73（让球-2 斯图加特vs维京@2.18 + 曼联vs萨巴赫@1.71）。
- **昨日回收**：09-08 T1 命中（投¥4 派¥8.4，净 +¥4.4）；T2 pending（腿未全开）——回收→判定→出票全链在云端一次跑通。
- **本机兜底在位**：`DailyBettingAdvisor` 状态「就绪」，下次运行 12:45；预检到当日批次已存在应 5 秒待命退出（待当日核）。

### 经验沉淀（以后参考）

1. **6 段 Quartz cron 是 Gitee Go 社区版唯一定时姿势**，且改后必须网页编辑器保存——09-08 血泪 + 09-09 准点双证。
2. **无 SLA 链路的重试必须配 fail-fast**：裸 retry×5 遇半死链路 = 空烧额度 80 分钟；git 原生 `http.lowSpeedLimit/Time` 零依赖解决，无需 timeout 命令/镜像源改造。
3. **验收看三处**：origin/main 的 `[cloud-primary]` 提交、Gitee `status` 分支当日摘要（含逐条时间线）、本机 schtasks 兜底状态——三绿才算闭环。
4. 云跑一次实际耗时 6.5 分钟（含 2 次掐断重试），净链路正常时约 3 分钟，月额度消耗仍在 ~120 分钟量级，安全。

### 当日复核（09-09 下午，M4 四步全绿）

- ✅ **本机兜底静默让路**：日志 `[2026-09-09 12:45:09] 当日批次已存在(2026-09-09, 云已出票), 本机待命退出`，全程 5 秒（12:45:04→12:45:09），无引擎运行、无 commit/push；origin/main 保持 `2325349`。至此兜底三条路径（09-07 让路 / 09-08 顶跑 / 09-09 让路）均实战验证。
- ✅ **CF Pages 镜像重建**：实测 `https://worldcup2026-12d.pages.dev/data/daily-advice.json` 最后批次 date=2026-09-09、generatedAt=`2026-09-09T03:36:50.292Z`（北京 11:36:50）——与云端引擎写盘时间**同秒对齐**，铁证重建随 `2325349` 完成。
- 附带：兜底任务 pull 时对未提交文档改动 autostash 后原样归还（`dd10ecb`），工作区无损。

### 遗留观察项（D2~D7）

- ✅ **09-10 D2 全绿（休战日）**：云定时 **11:30:20 准点触发**（误差 20 秒），11:33:26 落库 `d7ad58c [cloud-primary]`。status 分支时间线铁证：`start 11:30:20 → selftest PASS (31 用例) 11:33:25 → engine exit=0 11:33:26 → GitHub 回写成功 11:33:30`，全程 **3 分 10 秒**，`result=pushed`，本次链路顺畅 STALL 护栏未触发。引擎判定国际比赛日窗口白名单无候选 → `rest: true` **休战入档**（第 9 批，0 票——休战=正常产出，零候选不硬出票）。本机 12:45 兜底预检让路：日志 `[2026-09-10 12:45:08] 当日批次已存在(2026-09-10, 云已出票), 本机待命退出`，全程 4 秒（12:45:04→12:45:08），无引擎运行、无 commit，origin/main 保持 `d7ad58c`。
- ⬜ 09-11 ~ 09-15：云绿连跑 D3~D7，全绿则 M4 通过标准达成（断云顶替已于 09-08 完成、四步验收已于 09-09 全绿），转 M5 收尾归档。
